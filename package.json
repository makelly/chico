{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "A simple HTTP mock server ",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Kelly",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
